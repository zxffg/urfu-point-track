## Quizbot
это консольное Java-приложение для проведения викторин в терминале. Первая версия бота умела задавать вопросы, которые 
проверяли на знания сталиц. Проект создан в рамках учебного проекта.

### Сборка
1. Проверьте, что у вас JDK26
2. `git clone https://github.com/zxffg/urfu-java-cli-bot`
3. `mvn compile` или используйте IDE IntelliJ
4. `javac Main.main`
5. `./main.class`

### Функции бота
* /h и /help - описание бота и поддерживаемые команды.
* /q - выход из цикла
```txt
Привет! Я бот-опросник. 
Помогу проверить знания, пройти тест или просто весело провести время! Готов к первому вопросу?
Возникли трудности? Справка /help или /h

Столица Франции?
> париж

Верно!
Столица России?

> м0сква
Неверно!!
```
Используемый стек: JDK26, git

### Архитектура проекта
```txt
quizbot/
├── .mvn/                        # Файлы Maven Wrapper
├── src/                         # Исходный код
│   └── main/
│       ├── java/                # Основной Java-код
│       │   ├── console/
│       │   │   └── ConsoleRunner.java    # Скрипт запуска классов в терминале
│       │   ├── dialog/
│       │   │   ├── DialogEngine.java     # Логика ответов
│       │   │   └── UserSession.java      # Движок, связанный с пользователем
│       │   ├── questions/
│       │   │   ├── Question.java         # Класс с полями: вопрос - ответ
│       │   │   └── QuestionBank.java     # Класс "банк" вопросов
│       │   └── Main.java                 # Точка входа в приложение
├── .gitignore                   # Исключения для Git
├── pom.xml                      # Конфигурация Maven
└── README.md                    # Описание проекта
```
Описание пакетов с классами, которые являются главным функционалом бота:
├── [questions/](src/main/java/questions/)     
**Question** - класс с полями "question" и "answer", что по сути является хэшмапой.    
**QuestionBank** - класс с полями "счетчик" и "список элементов типа **Question**". В конструкторе создаются пары с 
вопросом и ответом на него. Переменная счетчика указывает на текущий вопрос, при выходе за диапазон, она 
закольцовывается, что предотвращает ошибки и обеспечивает бесконечный цикл.  
        &darr; &darr; &darr;  
├── [dialog/](src/main/java/dialog/)  
**DialogEngine** - тут находится главное ветвление, которое отвечает за вывод вопроса, справки и правильный/неправильный
ответ.  
**UserSession** - в этом классе создаются сессия пользователей по уникальному ID (потребуется в следующих задачах).  
        &darr; &darr; &darr;  
├── [console/](src/main/java/console/)  
**ConsoleRunner** - содержит метод `run()`, который содержит бесконечный цикл и получает ответы пользователя.

Стрелки обозначают слои изоляции: класс Question ничего не знает про класс ConsoleRunner (например).
### Формулировка учебной задачи
> При старте диалога, чат-бот рассказывает, что он умеет и как с ним взаимодействовать.
Затем в бесконечном цикле задает вопрос, получает от пользователя ответ и оценивает
его правильность, задаёт следующий вопрос и так далее. В любой момент можно
командой \help попросить бота ещё раз рассказать о себе, и пояснить как с ним
взаимодействовать.
Общаться с ботом можно через консоль. К организации кода предъявляются следующие
требования:
> 1. Код, реализующий хранилище вопросов или их генератор, должен находиться в
   отдельном классе от кода, реализующего логику диалога. Причем классы,
   реализующие хранилище / генератор вопросов не должны зависеть от классов,
   реализующих логику диалога.
> 2. Аналогично по разным классам должен быть разделен код, реализующий логику
   диалога и код, работающий с консолью. Причем классы логики не должны
   зависеть от классов с консолью.
   Тематику вопросов, вид ответов и способ оценивания ответов студенты придумывают
   сами. Начать стоит с чего-то максимально простого.
   Эта и все следующие задачи должны быть покрыты модульными тестами.